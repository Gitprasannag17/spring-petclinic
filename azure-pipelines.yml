# Maven
# Build your Java project and run tests with Apache Maven.
# Add steps that analyze code, save build artifacts, deploy, and more:
# https://docs.microsoft.com/azure/devops/pipelines/languages/java

trigger:
- main

pool:
  default

steps:

- task: Bash@3
  inputs:
    targetType: 'inline'
    script: |
      curl -X GET "http://20.41.251.44:9000" || exit 1 \
           -H "Authorization: Bearer 'sqa_bf51ea8808ec244dc4eb7f0c7999f9dc9d682145'" \
           -o output.json
